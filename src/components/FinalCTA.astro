<section class="final-cta" id="subscribe">
  <div class="final-cta-inner">
    <h2>Stop shipping experiments that don't <span class="em">survive</span></h2>
    <p>Get the economics lens — free, biweekly, no fluff.</p>
    <a href="https://causalera.substack.com" class="btn cta-btn" target="_blank" rel="noopener">Subscribe Free</a>
  </div>
</section>

<style>
  .final-cta{
    position:relative;
    background:var(--dark-surface);
    padding:100px 32px;
    text-align:center;
    overflow:hidden;
  }
  .final-cta::before{
    content:'';position:absolute;inset:0;
    background:
      radial-gradient(ellipse 70% 60% at 50% 100%, rgba(61,59,142,0.5) 0%, transparent 70%),
      radial-gradient(ellipse 50% 40% at 80% 30%, rgba(212,85,106,0.1) 0%, transparent 50%);
  }
  .final-cta-inner{position:relative;z-index:2}
  .final-cta h2{
    font-family:'DM Sans',sans-serif;font-weight:700;
    font-size:32px;line-height:1.15;
    color:var(--white);
    max-width:600px;margin:0 auto 16px;
  }
  .final-cta h2 .em{color:var(--accent)}
  .final-cta p{
    font-size:17px;color:var(--light-muted);
    max-width:480px;margin:0 auto 36px;
  }
  .cta-btn{
    display:inline-block;
    text-decoration:none;
    padding:14px 32px;
    font-size:18px;
  }

  @media(max-width:768px){
    .final-cta h2{font-size:32px}
  }
</style>
