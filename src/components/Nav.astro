<nav class="nav">
  <div class="nav-inner">
    <a class="wordmark" href="/">Causalera</a>
    <div class="nav-links">
      <a href="/newsletter">Newsletter</a>
      <a href="/contact">Contact</a>
      <a href="/#subscribe" class="nav-cta">Subscribe</a>
    </div>
  </div>
</nav>

<style>
  .nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    background:rgba(26,24,64,0.92);
    backdrop-filter:blur(12px);
    -webkit-backdrop-filter:blur(12px);
    border-bottom:1px solid rgba(74,71,165,0.3);
    transition:background 0.3s;
  }
  .nav-inner{
    max-width:1100px;margin:0 auto;padding:16px 32px;
    display:flex;justify-content:space-between;align-items:center;
  }
  .wordmark{
    font-family:'DM Sans',sans-serif;font-weight:700;font-size:22px;
    color:var(--white);text-decoration:none;letter-spacing:0.3px;
  }
  .nav-links{display:flex;gap:28px;align-items:center}
  .nav-links a{
    font-family:'DM Sans',sans-serif;font-size:17px;font-weight:600;
    color:var(--light-muted);text-decoration:none;
    transition:color 0.2s;
  }
  .nav-links a:hover{color:var(--white)}
  .nav-cta{
    font-family:'DM Sans',sans-serif;font-size:17px;font-weight:600;
    color:var(--white)!important;background:var(--accent);
    padding:8px 20px;border-radius:6px;text-decoration:none;
    transition:background 0.2s;opacity:1!important;
  }
  .nav-cta:hover{background:var(--accent-hover)}

  @media(max-width:768px){
    .nav-links a:not(.nav-cta){display:none}
  }
</style>
