<section class="hero">
  <div class="hero-inner">
    <div class="hero-badge">Economics &times; Experimentation &times; AI</div>
    <h1>The economics lens your <span class="em">experiments</span> are missing</h1>
    <p class="hero-sub">A newsletter for data leaders who want to make decisions that survive contact with the real world — not just pass a significance test.</p>
    <a href="https://causalera.substack.com" class="btn hero-btn" target="_blank" rel="noopener">Subscribe Free</a>
    <p class="hero-proof">Free, biweekly. Join data scientists, economists, and product leaders.</p>
  </div>
</section>

<style>
  .hero{
    position:relative;
    background:var(--dark-surface);
    padding:160px 32px 120px;
    overflow:hidden;
  }
  .hero::before{
    content:'';position:absolute;inset:0;
    background:
      radial-gradient(ellipse 80% 60% at 20% 80%, rgba(61,59,142,0.4) 0%, transparent 70%),
      radial-gradient(ellipse 60% 50% at 85% 20%, rgba(212,85,106,0.15) 0%, transparent 60%),
      radial-gradient(ellipse 100% 80% at 50% 0%, rgba(74,71,165,0.3) 0%, transparent 50%);
  }
  .hero::after{
    content:'';position:absolute;
    right:-40px;top:50%;transform:translateY(-50%);
    width:480px;height:480px;
    border:1px solid rgba(74,71,165,0.2);
    border-radius:50%;
    opacity:0.4;
    animation:drift 20s ease-in-out infinite;
  }
  @keyframes drift{
    0%,100%{transform:translateY(-50%) scale(1)}
    50%{transform:translateY(-48%) scale(1.04)}
  }
  .hero-inner{
    position:relative;z-index:2;
    max-width:720px;margin:0 auto;
  }
  .hero-badge{
    display:inline-block;
    font-family:'DM Sans',sans-serif;font-size:17px;font-weight:600;
    letter-spacing:1.5px;text-transform:uppercase;
    color:var(--accent);
    margin-bottom:28px;
    padding:6px 14px;
    border:1px solid rgba(212,85,106,0.3);
    border-radius:20px;
    animation:fadeUp 0.8s ease both;
  }
  .hero h1{
    font-family:'DM Sans',sans-serif;font-weight:700;
    font-size:48px;line-height:1.1;
    color:var(--white);
    margin-bottom:24px;
    letter-spacing:-0.5px;
    animation:fadeUp 0.8s ease 0.1s both;
  }
  .hero h1 .em{
    color:var(--accent);
  }
  .hero-sub{
    font-size:17px;line-height:1.65;
    color:var(--light-muted);
    max-width:560px;
    margin-bottom:40px;
    animation:fadeUp 0.8s ease 0.2s both;
  }
  .hero-btn{
    display:inline-block;
    text-decoration:none;
    padding:14px 32px;
    font-size:18px;
    animation:fadeUp 0.8s ease 0.3s both;
  }
  .hero-proof{
    margin-top:20px;
    font-family:'DM Sans',sans-serif;font-size:17px;
    color:var(--light-muted);
    animation:fadeUp 0.8s ease 0.4s both;
  }

  @media(max-width:768px){
    .hero{padding:130px 20px 80px}
    .hero h1{font-size:32px}
  }
</style>
